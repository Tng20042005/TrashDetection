export interface User {
    username: string;
    email: string;
    access_token: string;
    token_type: string;
}

export interface UserInfo {
    username: string;
    email: string;
    role: string;
}